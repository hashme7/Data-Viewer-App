export interface Store {
  id: number;
  store: string;
  city: string;
  state: string;
}
  export interface SKU {
    id: string,
    label: string,
    class: string,
    department: string,
    price: number,
    cost:number,
  }